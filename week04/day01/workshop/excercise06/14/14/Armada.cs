using System.Collections.Generic;

namespace _14
{
    public class Armada
    {
        private List<Ship> armada;

        Armada()
        {
            armada = new List<Ship>();
        }

        public bool War(Armada other)
        {
            while (this.Count > 0 && other.Count > 0)
            {

            }
        }
    }
}